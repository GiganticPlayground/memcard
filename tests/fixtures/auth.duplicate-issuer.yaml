# Invalid: a verified payload is traced back to its strategy by `iss`.
auth:
  - type: hs256
    issuer: https://players.test
    secret: ${env:TEST_PLAYER_SECRET}
  - type: hs256
    issuer: https://players.test
    secret: ${env:TEST_INTERNAL_SECRET}
